---
import LinkButton from "./LinkButton.astro"

const { name, description, href } = Astro.props;
---

<div
    class="bg-accent rounded-md flex flex-row items-center gap-[16px] rounded-lg py-[8px] px-[16px] w-fit h-fit"
>
    <img class="" src="./src/assets/placeholder.svg" alt="LevelSmith logo" />
    <div class="self-stretch border-l-[1px] border-primary"></div>
    <div class="flex flex-col gap-[4px]">
        <h3 class="text-xl font-medium">{name}</h3>
        <p>{description}</p>
    </div>
    <LinkButton name="Learn More" href={href} />
</div>
